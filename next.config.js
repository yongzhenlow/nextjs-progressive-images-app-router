/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'source.unsplash.com',
      'images.unsplash.com',
      'cdn.shibe.online',
      'images.ctfassets.net',
    ],
  },
}

module.exports = nextConfig
